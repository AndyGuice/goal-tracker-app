import React, { useEffect } from 'react';

const AddTask = (props: any) => {

  useEffect(() => {
    console.log('Add Task');
  });
  return (
    <div>
      Add Task
    </div>
  );
};

export default AddTask;
